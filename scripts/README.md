# scripts/

## Daily

- dev.ps1 — start local stack
- check.ps1 — run all pre-PR checks
- lint.ps1 — lint/format checks
- status.ps1 — show container status
- down.ps1 — stop stack

## Backend

- migrate.ps1 — run migrations
- superuser.ps1 — create admin user
- logs-backend.ps1 — tail backend logs

## Frontend

- format-frontend.ps1 — format frontend
- logs-frontend.ps1 — tail frontend logs

## Data

- seed-projects.ps1 — seed sample projects
